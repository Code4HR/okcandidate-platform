'use strict'
/* global describe, it */

const assert = require('assert')

describe('SurveyResult Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['SurveyResult'])
  })
})
