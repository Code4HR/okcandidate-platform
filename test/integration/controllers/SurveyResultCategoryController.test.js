'use strict'

const assert = require('assert')

describe('SurveyResultCategoryController', () => {
  it('should exist', () => {
    assert(global.app.controllers.SurveyResultCategoryController)
  })
})
