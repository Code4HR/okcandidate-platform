'use strict';

import React, { Component } from 'react';
import PropTypes from 'prop-types';

import Card from './../atoms/Card';

class SurveyCreatorCandidates extends Component {
    render() {
        return (
            <Card>
                <pre>SurveyCreatorCandidates</pre>
            </Card>
        )
    }
}

SurveyCreatorCandidates.propTypes = {};

export default SurveyCreatorCandidates;